# Доступная Навигационная Система

Система умной навигации для людей с ограниченными возможностями в Тульской области. Предоставляет персонализированные маршруты с учетом объектов доступности, используя данные из официальных источников.

## 🚀 Возможности

### Основные функции
- **Умное построение маршрутов**: Автоматическое включение объектов доступности в маршрут с оптимизацией по расстоянию
- **Поддержка категорий инвалидности**:
  - Колясочники (пандусы, лифты, широкие двери, доступная парковка)
  - Слабовидящие (тактильная плитка, звуковые светофоры, кнопки вызова)
  - Пользователи трости (поручни, понижение бордюров)
- **Интерактивная карта**: MapLibre GL JS с возможностью клика для выбора адреса
- **Умное автодополнение**: Предложения адресов из базы данных и OpenStreetMap с клавиатурной навигацией
- **Геолокация**: Определение текущего местоположения с обратным геокодированием
- **Интеграция XML данных**: Автоматическая загрузка 2868 организаций и 2831 объекта инфраструктуры из официальных источников

### Доступность
- **Голосовое сопровождение**: Озвучивание интерфейса для слепых пользователей
- **Режим для слабовидящих**: Высокий контраст и улучшенная видимость
- **Клавиатурная навигация**: Полная поддержка клавиатуры в формах и автодополнении
- **Клик на карту**: Выбор адреса путем клика на карте с автоматическим заполнением
- **Предупреждения**: Уведомления об организациях, не обслуживающих выбранный тип инвалидности

### Администрирование
- Панель администратора для модерации объектов доступности
- Добавление объектов через веб-интерфейс
- Управление пользователями

## 📋 Требования

- Python 3.8+
- SQLite (входит в Python)
- Интернет-соединение (для карт и геокодирования)

## 🛠 Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd Accessible-Navigation-System
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Запуск приложения
```bash
python map_creator.py
```

Приложение будет доступно по адресу: http://127.0.0.1:5000

## 🐳 Запуск с Docker

### Сборка и запуск
```bash
docker-compose up --build
```

Приложение будет доступно по адресу: http://localhost:5000

## 📄 XML Данные

Система поддерживает автоматическую загрузку данных из XML файлов:

### Файлы данных
- `xml/Файл_соцподдержка_1.xml` - Организации социальной поддержки (2868 записей)
- `xml/Файл_соцподдержка_2.xml` - Инфраструктура доступности (2831 объект)

**Примечание:** Эти XML файлы не включены в репозиторий (исключены через .gitignore) из-за размера и конфиденциальности данных. Для полной функциональности системы необходимо вручную получить эти файлы из официальных источников Тульской области и поместить их в директорию `xml/`.

### Автоматическая обработка
- Парсинг XML с извлечением организаций и объектов
- Автоматическое присвоение типов доступности на основе названий
- Фильтрация организаций по категориям инвалидности
- Обновление базы данных при каждом запуске

## 📖 Использование

### Построение маршрута
1. Выберите тип ограничений мобильности
2. Введите адрес отправления (или используйте геолокацию с "текущий")
3. Введите адрес назначения (с умным автодополнением из базы данных)
4. Или кликните на карте для автоматического заполнения адреса
5. Нажмите "Построить маршрут"
6. Система покажет предупреждение, если адрес не найден

### Автодополнение адресов
- Начинайте вводить адрес - появятся предложения
- Используйте стрелки ↑↓ для навигации, Enter для выбора, Escape для отмены
- Предложения включают существующие адреса из базы данных
- Организации помечаются предупреждением, если не обслуживают выбранный тип инвалидности

### Добавление объектов доступности
1. Перейдите по прямой ссылке /submit
2. Заполните форму с типом объекта, описанием и адресом
3. Прикрепите фото
4. Отправьте на проверку

### Администрирование
1. Перейдите на /admin
2. Войдите с учетными данными (admin/admin)
3. Одобряйте или отклоняйте заявки на объекты

## 🎯 API Endpoints

### Основные
- `GET /` - Главная страница с картой
- `POST /api/route` - Построение маршрута с поддержкой координат
- `GET /api/suggest_address` - Предложения адресов из БД и OSM
- `GET /api/reverse_geocode` - Обратное геокодирование для клика на карту
- `GET /api/organizations` - Список организаций с фильтрацией по типу инвалидности

### Добавление объектов
- `GET /submit` - Форма добавления объекта
- `POST /api/submit` - Отправка объекта на проверку

### Администрирование
- `GET /admin` - Панель администратора
- `POST /api/approve/<id>` - Одобрение объекта
- `POST /api/reject/<id>` - Отклонение объекта

## 🗄 База данных

Приложение использует SQLite базу данных `accessibility.db` с таблицами:
- `accessibility_objects` - Одобренные объекты доступности (2831 объект из XML)
- `user_submissions` - Заявки на добавление объектов
- `admins` - Администраторы системы

### Автоматическая загрузка данных
При первом запуске система автоматически:
1. Парсит XML файлы с организациями и инфраструктурой
2. Создает объекты доступности на основе анализа названий
3. Заполняет базу данных реальными данными Тульской области

База данных создается автоматически при первом запуске.

## 🎨 Доступность

### Для слепых пользователей
- Включите "Голосовое сопровождение" в заголовке страницы
- Используйте клавиатуру для навигации
- Голосовые подсказки при взаимодействии с элементами

### Для слабовидящих
- Включите "Режим для слабовидящих" для высокого контраста
- Увеличьте масштаб браузера при необходимости

## 🔧 Настройка

### Переменные окружения
- `FLASK_ENV` - Режим работы (development/production)
- `SECRET_KEY` - Секретный ключ для сессий

### Кастомизация
- Измените координаты центра карты в `map_creator.py`
- Добавьте новые типы объектов в `MobilityType` и `AccessibilityFeature`
- Настройте приоритеты в `feature_priorities`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи консоли браузера
2. Убедитесь в наличии интернет-соединения
3. Проверьте правильность установки зависимостей
