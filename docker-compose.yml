version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./accessibility.db:/app/accessibility.db
    environment:
      - FLASK_ENV=development